It is a very basic project of making embedding of a document.txt and them applying the TurboQuant algorithm on the embedding to reduce the size of the embedding. This is used in RAG (Retrieval-Augmented Generation)
Checking the difference between the outputs before and after the quantisation of the embeddings.
